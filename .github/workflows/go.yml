on: push
name: go-site24x7-ci
jobs:
  checks:
    name: run
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master

    - name: run
      uses: cedrickring/golang-action@1.5.2
      with:
        args: go test --cover ./... 
